module FsCommon
(*
// http://fussy.web.fc2.com/algo/algo10-6.htm

/// ニュートン - ラフソン法を用いてべき乗根以上の整数最小値を求める
let root c e = 
    // c <<= 64
    // let ans = c

    // while 1
    //   let fx = ans
    //   fx.pow e
    //   let dfx = ans
    //   dfx ** (e-1)
    //   dfx *= e
    //   if fx < dfx then break;

    // ans -= fx / dfx
    ()

/// 素数のべき乗で表せる数かどうかをチェックする
let isPow n =
    if 4I < n then false
    else
        let (m,k) = 2I,0I
        // while m < n
        // m <<= 1;
        // if m = n then return true
        // k += 1

        // for i = k; i > 0; i -= 1 )
        // r = root(n,i)
        // if (r.pow(i) = n) return true
        //return false
*)
